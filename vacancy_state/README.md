# Versioned vacancy state

This directory contains generated, auditable state for incremental vacancy ingestion. Do not edit records, chunks, manifest entries or indexes manually.

Canonical vacancy inputs live in `GPTW/` and `Vacantes/`. Canonical professional evidence lives in `experience/`. Corrections belong in those source layers; this state is then regenerated incrementally.
