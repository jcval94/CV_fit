# Work final-review handoff

This directory is generated by the CV pipeline and is the queue consumed by the scheduled final-review task.

Each vacancy package contains:

- `review_context.json` — automated quality, headhunter, evidence-coverage, unsupported-requirement and presentation diagnostics.
- `evidence_snapshot.json` — the exact public-safe RAG evidence cited by the proposed CV, including claim constraints.
- `vacancy.md` — a cleaned human-readable version of the employer JD.
- `vacancy.json` — canonical structured vacancy data.
- `cv_proposed.json` — current evidence-grounded CV proposal.
- `cv_proposed.html` — current rendered proposal.
- `html_base.html.j2` — visual/template baseline.
- `public_identity.yaml` — identity fields safe to publish in this public repository.
- `cover_letter_proposed.md` — generated cover letter when available.
- `prompt.md` — final-review contract.
- `handoff.json` — manifest, status, KPI and links.

The final reviewer writes `final.html` and `review_notes.md`. The **Final CV render** GitHub Action then creates `final.pdf`, `final.png`, and `final_report.json`.

## Queue order

`index.json` lists pending candidates first, then sorts by:

1. source fit descending;
2. source date descending.

## Privacy boundary

The repository is public. Handoff and final artifacts may only use `public_identity.yaml`. Private email, phone and address must never be committed here. A private-contact send version, if desired, must be created outside the public repository.
